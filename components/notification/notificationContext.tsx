import React, { createContext } from "react";


const NotificationContext = createContext(null);


export const NotificaitonProvider = ({children}:{children:React.ReactNode}) => {

    const [notifications]

}